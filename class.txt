class Table
{
    private:
    int r,c;
    int num_mines;
    bool bomb[r][c];
    bool visible[r][c];
    bool q_mark[r][c];
    bool flag[r][c];
    short number[r][c];
    bool loss;
    char toPrint[r][c]; // we simply need to print this array. It will store characters that will be shown. We need to make changes to this array too.
    // like for bomb it will show 'B', For the number it will show the '1', '2', etc. For the empty boxes (which have neither bomb nor any bomb surrounded)it can show 'O' or '0' 
    // and for boxes yet to be opened it can show 'T' (or any other character for that matter)
    
    public:
    Table(int rows, int cols, int num, int i_ex, int j_ex)
    {
        r=rows;
        c=cols;
        num_mines=num;
        set_mines(i_ex,j_ex);
        set_num();
    }
    
    void set_mines(int i,int j)
    {
	//fill this up
    }
    void set_num()
    {
	//fill this up
    }
    void flag(int i,int j)
    {
        flag[i][j]=true;
    }
    void question(int i,int j)
    {
        q_mark[i][i]=true;
    }
    void reveal_from(int i, int j)
    {
	if(bomb[i][j])
        {
            loss=true;return;
        }
	//fill this up        
    }
    
    boolean check_win()
    {
	//fill this up    
    }
    boolean check_loss()
    {
	return loss;       
    }
    
    void print()
    {
	//fill this up        
    }
    
}
